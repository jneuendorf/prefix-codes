# prefix-codes


Python prefix codes codecs:

- with explicit codeword table
- Huffman
- Shannon-Fano-Elias

126 lines of code (`cat **/*.py | grep -v '^$' | wc -l`).


## How to use

```bash
python3 ./main.py

make test
```


## Requirements

- Python >= 3.10
